package tests

import ("testing"

)
func TestElection(t *testing.T) {
	// create raft instances, create services from that, put that on a new server and add to the network
	// each raft instance is expected to connect to another raft instance to send packets over

	t.Run("ideal conditions", func(t *testing.T) {

	})
}